# git subrepo clone https://github.com/prh/rules.git
# npm run textlint
# npm run -s lint
# npm run -s fixlint

# docker run -v ${PWD}:/doc/ mnishikizawa:test ./demo.md
# docker-compose run textlint /data/demo.md
